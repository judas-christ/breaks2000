{
  "name": "breaks2000",
  "version": "2.2.0",
  "description": "Simple responsive elements script",
  "devDependencies": {
    "rimraf": "^2.4.1",
    "rollup": "^0.41.4",
    "rollup-plugin-buble": "^0.15.0",
    "rollup-plugin-replace": "^1.1.1",
    "uglifyjs": "^2.4.10"
  },
  "main": "dist/index.js",
  "module": "dist/module.js",
  "jsnext:main": "dist/module.js",
  "scripts": {
    "bundle": "rollup -c --mobile_first",
    "minify": "uglifyjs dist/breaks2000.js -o dist/breaks2000.min.js -c drop_console,negate_iife=false --lift-vars -mt --comments \"/\\/*!/\"",
    "build": "rimraf dist && mkdir dist && npm run bundle && npm run minify",
    "prepublish": "npm run build"
  },
  "author": "Daniel Hägglund <daniel.hagglund@gmail.com>",
  "repository": {
    "type": "git",
    "url": "https://github.com/judas-christ/breaks2000.git"
  },
  "files": [
    "dist/"
  ],
  "license": "MIT"
}
